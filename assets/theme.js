/* Background colour is fixed for now — the randomizer is commented
   out in config.js. To bring it back: uncomment the BACKGROUNDS
   list there, then uncomment the block below and delete the fixed
   assignment above it. */

document.body.style.background = "#B893AC";
document.body.style.color = "#16150F"; // kept dark on purpose

// const pick = (list) => list[Math.floor(Math.random() * list.length)];
//
// function applyBackground() {
//   document.body.style.background = pick(BACKGROUNDS).hex;
// }
//
// applyBackground();
// document.addEventListener("DOMContentLoaded", applyBackground);
